# Agents (`Application/AI/Agents/`)

LLM agents for chat turns.

| Naming | Role |
|--------|------|
| `{Domain}TriageAgent` | Triage system (ingest → prioritize → act). Example: `EmailTriageAgent`. |
| `{Role}Agent` | Non-triage (guide, router). Example: `AssistantAgent`. |
