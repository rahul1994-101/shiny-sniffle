# Workflows (`Application/AI/Workflows/`)

Multi-step orchestration that is **not** a single LLM agent turn (e.g. cross-system pipelines).

System-specific scripted flows live under `Triages/{System}/Workflows/` first; promote here when shared across systems.
